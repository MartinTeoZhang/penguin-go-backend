# tomcat
server:
  port: 8002
# spring boot admin
spring:
  application:
    name: Penguin Backup Service
  boot:
    admin:
      client:
        url: "http://localhost:8000"
management:
  endpoints:
    web:
      exposure:
        include: "*"
# backup datasource
penguin:
  backup:
    datasource:
      host: localhost
      userName: root
      password: 123456
      database: penguin